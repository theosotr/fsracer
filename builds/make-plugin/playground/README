Makefile with missing dependency
================================

* Missing **s2.c** or use `%.o: %.c`

```make
$(OBJECTS): main.c s1.c s1.h s2.h
        $(CC) -c main.c s1.c s2.c $(CFLAGS)
```

Examples
========

* `make` 

* `make -f mOBJ` 

* `strace -s 200 -f make 2> traces`

* `make SHELL='$(info \#BEGIN)$(warning $*,$@,$^)./post-shell'`

* `strace -s 200 -f -e "$(tr -s '\r\n' ',' < syscalls.txt | sed -e 's/,$/\n/')" -o make.traces make SHELL='$(warning ##BEGIN## $*,$@,$^)./post-shell'`
